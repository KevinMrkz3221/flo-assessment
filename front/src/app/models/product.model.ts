export interface Product {
    id?: number;
    title: string;
    price: number;
    description: string;
    images: string;
}

export interface MyProduct {
    id?: number;
    id_api: number;
    title: string;
    price: number;
    description: string;
    image: string;
}